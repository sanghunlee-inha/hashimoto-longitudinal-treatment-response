# Scripts

`v35_4_clinical_benchmark_trajectory_decoupling.py` is the current locked clinical-benchmark and trajectory-decoupling analysis script.

`run_v35_4_windows.ps1` is the Windows launcher used with the authorized local project directory.

Before public release, add the final manuscript figure-generation scripts and any earlier analysis scripts that are required to reproduce the complete paper. Do not copy patient-level CSV/Parquet files into this repository.
